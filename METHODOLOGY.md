# 方法论

## 要解决的问题

直接让 AI 把事情全部做完，我完全不知道过程中发生了什么。
等到出问题时，面对的是一个我并不理解的系统，认知负担极大。

## 目标

让"我知道这个系统怎么运作"成为写代码的**前置条件**，而不是写完之后再去补的功课。

需要长期保持的理解包含两层：

- **需求上下文** —— 为什么有这个需求；需求需要上下文的支持
- **运行逻辑 + 数据流** —— 系统实际怎么运作

两者都必须在实现开始之前建立。

## 阶段划分

开发按顺序推进，理解在前，代码在后。

### 1. Spec-Driven-Development —— 对齐需求

这个阶段要回答的问题：

- **需求是什么** —— 要做什么
- **为什么有这个需求** —— 需求产生的上下文

此阶段不涉及实现方案。

### 2. Interface-Driven-Development —— 对齐结构

在需求确定之后、写代码之前，定下大体上怎么做 —— 用什么架构和结构。

这个阶段要回答的问题：

- **模块划分** —— 预期会有几个模块
- **模块职责** —— 每个模块的职责是什么
- **公开接口** —— 每个模块对外暴露什么能力
- **模块间互动** —— 模块之间有什么互动
- **数据流** —— 数据从哪来、中途变成什么、最后存到哪
- **业务流程** —— 如果有具体业务实现，大概流程是怎样的

精度要求是"大体运作逻辑"，不是逐行实现。

### 2.5 Attention-Driven-Development —— 局部下钻（可选）

**可选环节**，在 IDD 达成一致之后进行。

当我担心某个地方的具体实现时，在这里对该处扣细节，做特别说明。

典型场景：

- 延迟敏感的部分
- 流程敏感的业务

只有被特别说明的地方下钻到实现细节，其余部分仍保持 IDD 的精度。

### 3. plan

生成实现计划。本质是由一个聪明的模型分解任务，告诉执行的 agent 每一步需要做什么。

理论上这一步不再需要人工干预，但计划生成后人可以选择性地看一遍。

### 4. 实现

计划生成后**一次跑完**，中途不逐任务打断。

完成后做一次**总 review**。

---

阶段 3 与阶段 4 已有成熟工具承担，方法论不另行定义。当前使用 speckit 与 openspec 的 explore。
