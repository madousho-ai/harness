# skill

系统提示词里列着这个工作区可用的 skill。要动手做的事对上哪个就先加载哪个 —— 写第一个失败测试之前加载 test-driven-development，做普通 commit 之外的 git 操作之前加载 git-master，要按语法结构搜代码之前加载 ast-grep。

skill 装的是提示词放不下的流程细节，也是这个工作区自己的约定到达你的通道。提示词写「这个项目的东西长什么样」，skill 写「怎么操作」，两边都要。

skill 给的是通用做法，项目自己的文档给的是这一次必须做成什么样。两者冲突时照文档做，并说出你在哪儿偏离了 skill。
